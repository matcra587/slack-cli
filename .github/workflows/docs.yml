name: docs

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'includes/**'
      - 'zensical.toml'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

# GitHub Pages allows only one deployment at a time. Cancel any in-flight
# deploy when a newer commit lands.
concurrency:
  group: pages
  cancel-in-progress: true

permissions: {}

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    permissions:
      contents: read            # Required to check out the repo.
      pages: read               # Required for configure-pages to fetch the site's base URL.
    steps:
      - name: Checkout
        # clover: provider=auto constraint=minor tags=checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Configure Pages
        # clover: provider=auto constraint=minor tags=configure-pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - name: Set up Python
        # clover: provider=auto constraint=minor tags=setup-python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.x'

      - name: Install Zensical
        run: pip install zensical

      - name: Build site
        run: zensical build --clean --strict

      - name: Upload artifact
        # clover: provider=auto constraint=minor tags=upload-pages-artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site

  deploy:
    name: Deploy to Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write              # Required to publish the artifact to GitHub Pages.
      id-token: write           # Required for the Pages deploy action's OIDC token exchange.
    steps:
      - name: Deploy
        id: deployment
        # clover: provider=auto constraint=minor tags=deploy-pages
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
